---
- hosts:
    - controller
  # roles:
  #   - solo/controller
  vars:
    wifi_network_conf: |
      ssid="aroma espresso bar"
      key_mgmt=NONE
  tasks:
    - name: "wifi"
      import_role:
        name: solo/controller
        tasks_from: wifi.yml
